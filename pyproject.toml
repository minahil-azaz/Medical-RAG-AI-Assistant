[project]
name = "rag-medical-ai-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "langchain",
    # add other dependencies here
]

[tool.poetry]
package-mode = false
